---
import type { UserSessionPayload } from '../../utils/auth';

interface Props {
  user: UserSessionPayload | null;
}

const { user } = Astro.props;
---

<div class="interactive-material-p5 space-y-8" id="interactive-p5-root">

  <!-- SCENE 1: Rute Booth Job Fair -->
  <section class="p-5 rounded-2xl bg-gradient-to-br from-rose-500/10 via-pink-500/5 to-transparent border border-rose-500/20 shadow-sm relative overflow-hidden">
    <div class="absolute top-0 right-0 w-32 h-32 bg-rose-500/10 blur-3xl rounded-full"></div>
    
    <div class="relative z-10 space-y-4">
      <div class="flex items-center gap-2">
        <span class="p-2 rounded-lg bg-rose-500/20 text-rose-500 font-bold text-xs font-mono">SCENE 01</span>
        <h3 class="text-lg font-bold text-foreground">Pilih Rute Booth Job Fair</h3>
      </div>
      
      <p class="text-xs text-muted-foreground leading-relaxed">
        Pilih rute booth yang paling sesuai dengan bakat atau tujuan eksplorasi kariermu di acara Job Fair.
      </p>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
        <!-- Rute Options -->
        <div class="space-y-2">
          <button type="button" class="booth-btn w-full text-left p-3 rounded-lg border border-border bg-card hover:border-rose-500/50 transition-all flex items-center justify-between group focus-visible:outline-rose-400" data-target="logis">
            <span class="font-medium text-sm">🧩 Minat Logis</span>
            <span class="opacity-0 group-hover:opacity-100 transition-opacity">➔</span>
          </button>
          <button type="button" class="booth-btn w-full text-left p-3 rounded-lg border border-border bg-card hover:border-rose-500/50 transition-all flex items-center justify-between group focus-visible:outline-rose-400" data-target="visual">
            <span class="font-medium text-sm">🎨 Minat Visual</span>
            <span class="opacity-0 group-hover:opacity-100 transition-opacity">➔</span>
          </button>
          <button type="button" class="booth-btn w-full text-left p-3 rounded-lg border border-border bg-card hover:border-rose-500/50 transition-all flex items-center justify-between group focus-visible:outline-rose-400" data-target="analitis">
            <span class="font-medium text-sm">📊 Minat Analitis</span>
            <span class="opacity-0 group-hover:opacity-100 transition-opacity">➔</span>
          </button>
          <button type="button" class="booth-btn w-full text-left p-3 rounded-lg border border-border bg-card hover:border-rose-500/50 transition-all flex items-center justify-between group focus-visible:outline-rose-400" data-target="skill">
            <span class="font-medium text-sm">🎯 Tujuan Skill Awal</span>
            <span class="opacity-0 group-hover:opacity-100 transition-opacity">➔</span>
          </button>
          <button type="button" class="booth-btn w-full text-left p-3 rounded-lg border border-border bg-card hover:border-rose-500/50 transition-all flex items-center justify-between group focus-visible:outline-rose-400" data-target="tim">
            <span class="font-medium text-sm">🤝 Tujuan Kerja Tim</span>
            <span class="opacity-0 group-hover:opacity-100 transition-opacity">➔</span>
          </button>
        </div>

        <!-- Result Display -->
        <div id="booth-display" class="hidden h-full p-4 rounded-xl bg-rose-500/10 border border-rose-500/20 flex flex-col justify-center" role="status" aria-live="polite">
          <h4 id="booth-title" class="font-bold text-rose-500 mb-2">...</h4>
          <p id="booth-desc" class="text-sm text-foreground mb-3">...</p>
          <div class="mt-auto pt-3 border-t border-rose-500/20">
            <p id="booth-feedback" class="text-xs text-rose-600 dark:text-rose-400 font-medium italic">...</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- SCENE 2: Kualitas Pertanyaan Wawancara -->
  <section class="p-5 rounded-2xl bg-gradient-to-br from-violet-500/10 via-purple-500/5 to-transparent border border-violet-500/20 shadow-sm relative overflow-hidden">
    <div class="absolute bottom-0 right-0 w-40 h-40 bg-violet-500/10 blur-3xl rounded-full"></div>
    
    <div class="relative z-10 space-y-4">
      <div class="flex items-center gap-2">
        <span class="p-2 rounded-lg bg-violet-500/20 text-violet-500 font-bold text-xs font-mono">SCENE 02</span>
        <h3 class="text-lg font-bold text-foreground">Analisis Pertanyaan Wawancara</h3>
      </div>
      
      <p class="text-xs text-muted-foreground leading-relaxed">
        Apakah pertanyaan ini Kuat (spesifik & bermakna) atau Lemah (terlalu umum)? Klik kategori yang tepat untuk setiap pertanyaan.
      </p>

      <div class="space-y-4">
        <!-- Progress -->
        <div class="flex justify-between text-xs font-bold text-muted-foreground">
          <span>Progress: <span id="qa-progress">0/6</span></span>
          <span id="qa-score" class="text-violet-500">Skor: 0</span>
        </div>

        <!-- Question Card -->
        <div id="qa-card" class="p-6 rounded-xl border border-border bg-card text-center shadow-sm">
          <p id="qa-text" class="text-base font-medium text-foreground mb-6">"..."</p>
          <div class="flex justify-center gap-4">
            <button type="button" class="qa-btn px-6 py-2 rounded-lg bg-red-500/10 text-red-600 dark:text-red-400 border border-red-500/30 hover:bg-red-500/20 font-bold text-sm focus-visible:outline-red-400 transition-all" data-answer="lemah">
              📉 Lemah
            </button>
            <button type="button" class="qa-btn px-6 py-2 rounded-lg bg-green-500/10 text-green-600 dark:text-green-400 border border-green-500/30 hover:bg-green-500/20 font-bold text-sm focus-visible:outline-green-400 transition-all" data-answer="kuat">
              📈 Kuat
            </button>
          </div>
        </div>

        <!-- Feedback Area -->
        <div id="qa-feedback-area" class="hidden p-4 rounded-xl bg-secondary border border-border text-center" role="status" aria-live="polite">
          <h4 id="qa-feedback-title" class="font-bold mb-1">Benar!</h4>
          <p id="qa-feedback-desc" class="text-sm text-foreground mb-2">...</p>
          <p id="qa-feedback-lesson" class="text-xs text-muted-foreground italic">...</p>
          <button type="button" id="qa-next-btn" class="mt-3 px-4 py-1.5 bg-violet-500 text-white rounded-lg text-xs font-bold hover:bg-violet-600 transition-all">Lanjut ➔</button>
        </div>

        <!-- Completion -->
        <div id="qa-success" class="hidden p-4 rounded-xl bg-violet-500/10 border border-violet-500/30 text-center animate-in zoom-in" role="status" aria-live="polite">
          <div class="text-3xl mb-2">🎉</div>
          <h4 class="font-bold text-violet-500">Analisis Selesai!</h4>
          <p class="text-xs text-muted-foreground mt-1">Pertanyaan wawancara yang kuat menyebut fokus yang jelas dan menghasilkan wawasan yang dapat ditindaklanjuti.</p>
          <button type="button" id="qa-reset-btn" class="mt-3 px-4 py-1.5 bg-secondary text-foreground border border-border rounded-lg text-xs font-bold hover:bg-secondary/80 transition-all">Ulangi Latihan</button>
        </div>
      </div>
    </div>
  </section>

</div>

<script>
  // SCENE 1 Logic
  const boothData: Record<string, { title: string, desc: string, feedback: string }> = {
    'logis': {
      title: 'Minat Logis',
      desc: 'Kunjungi booth yang menjelaskan masalah nyata dan cara profesi menyelesaikannya.',
      feedback: 'Rute ini membantu menguji kecocokan dengan pemecahan masalah algoritma/backend.'
    },
    'visual': {
      title: 'Minat Visual',
      desc: 'Kunjungi booth yang menampilkan contoh karya, UI, dan produk nyata.',
      feedback: 'Rute ini membantu menguji kecocokan dengan UI/UX dan frontend visual.'
    },
    'analitis': {
      title: 'Minat Analitis',
      desc: 'Kunjungi booth yang membahas metrik, data, tantangan operasional, dan arsitektur.',
      feedback: 'Rute ini membantu menguji kecocokan dengan Data Science atau QA.'
    },
    'skill': {
      title: 'Tujuan Skill Awal',
      desc: 'Kunjungi booth yang menjelaskan bahasa pemrograman atau software pertama untuk dipelajari pemula.',
      feedback: 'Rute ini memberi arah pembelajaran (roadmap) awal yang konkret.'
    },
    'tim': {
      title: 'Tujuan Kerja Tim',
      desc: 'Kunjungi booth yang membahas cara bekerja dalam tim besar, DevOps, dan metodologi Agile.',
      feedback: 'Rute ini memperjelas realitas kolaborasi dan siklus rilis produk.'
    }
  };

  const boothBtns = document.querySelectorAll('.booth-btn');
  const boothDisplay = document.getElementById('booth-display');
  const boothTitle = document.getElementById('booth-title');
  const boothDesc = document.getElementById('booth-desc');
  const boothFeedback = document.getElementById('booth-feedback');

  boothBtns.forEach(btn => {
    btn.addEventListener('click', () => {
      const target = btn.getAttribute('data-target');
      if (target && boothData[target]) {
        boothBtns.forEach(b => b.classList.remove('ring-2', 'ring-rose-500', 'bg-rose-500/5'));
        btn.classList.add('ring-2', 'ring-rose-500', 'bg-rose-500/5');
        
        boothTitle!.textContent = boothData[target].title;
        boothDesc!.textContent = boothData[target].desc;
        boothFeedback!.textContent = `💡 ${boothData[target].feedback}`;
        
        boothDisplay!.classList.remove('hidden');
      }
    });
  });


  // SCENE 2 Logic
  const qaQuestions = [
    { text: "Masalah nyata apa yang paling sering diselesaikan di perusahaan ini?", type: "kuat", desc: "Pertanyaan mengarahkan narasumber pada contoh masalah dalam pekerjaan.", lesson: "Pertanyaan kuat: fokusnya spesifik dan mengungkap realitas profesi." },
    { text: "Tech stack apa yang pertama harus saya kuasai?", type: "kuat", desc: "Pertanyaan meminta arah bahasa pemrograman atau software untuk pemula.", lesson: "Pertanyaan kuat: jawabannya dapat ditindaklanjuti menjadi rencana belajar." },
    { text: "Apa kesulitan terbesar saat bekerja dalam tim besar?", type: "kuat", desc: "Pertanyaan menggali tantangan profesi dalam kolaborasi.", lesson: "Pertanyaan kuat: konteks timnya jelas dan relevan." },
    { text: "Tugas harian apa yang paling sering dilakukan?", type: "kuat", desc: "Pertanyaan meminta contoh kegiatan kerja yang berulang.", lesson: "Pertanyaan berkualitas: jawabannya membantu memahami keseharian profesi." },
    { text: "Profesi ini bagus, kan?", type: "lemah", desc: "Pertanyaan hanya mengundang jawaban setuju atau tidak setuju (Yes/No question).", lesson: "Pertanyaan lemah: terlalu umum dan tidak menghasilkan wawasan yang spesifik." },
    { text: "Gajinya besar?", type: "lemah", desc: "Pertanyaan tidak menentukan konteks pengalaman atau tanggung jawab.", lesson: "Pertanyaan lemah: kurang spesifik untuk memahami rentang kompensasi secara bermakna." }
  ];

  // Shuffle array
  const shuffledQa = [...qaQuestions].sort(() => Math.random() - 0.5);
  let currentQaIndex = 0;
  let qaScore = 0;

  const qaText = document.getElementById('qa-text');
  const qaCard = document.getElementById('qa-card');
  const qaFeedbackArea = document.getElementById('qa-feedback-area');
  const qaFeedbackTitle = document.getElementById('qa-feedback-title');
  const qaFeedbackDesc = document.getElementById('qa-feedback-desc');
  const qaFeedbackLesson = document.getElementById('qa-feedback-lesson');
  const qaNextBtn = document.getElementById('qa-next-btn');
  const qaSuccess = document.getElementById('qa-success');
  const qaProgress = document.getElementById('qa-progress');
  const qaScoreEl = document.getElementById('qa-score');
  const qaResetBtn = document.getElementById('qa-reset-btn');

  function renderQa() {
    if (currentQaIndex >= shuffledQa.length) {
      qaCard!.classList.add('hidden');
      qaFeedbackArea!.classList.add('hidden');
      qaSuccess!.classList.remove('hidden');
      return;
    }
    
    qaCard!.classList.remove('hidden');
    qaFeedbackArea!.classList.add('hidden');
    qaText!.textContent = `"${shuffledQa[currentQaIndex].text}"`;
    qaProgress!.textContent = `${currentQaIndex + 1}/${shuffledQa.length}`;
    qaScoreEl!.textContent = `Skor: ${qaScore}`;
  }

  document.querySelectorAll('.qa-btn').forEach(btn => {
    btn.addEventListener('click', () => {
      const answer = btn.getAttribute('data-answer');
      const correct = shuffledQa[currentQaIndex].type;
      
      qaCard!.classList.add('hidden');
      qaFeedbackArea!.classList.remove('hidden');
      
      qaFeedbackDesc!.textContent = shuffledQa[currentQaIndex].desc;
      qaFeedbackLesson!.textContent = `💡 ${shuffledQa[currentQaIndex].lesson}`;

      if (answer === correct) {
        qaScore += 100;
        qaFeedbackTitle!.textContent = '✅ Benar!';
        qaFeedbackTitle!.className = 'font-bold mb-1 text-green-500';
      } else {
        qaFeedbackTitle!.textContent = '❌ Kurang Tepat';
        qaFeedbackTitle!.className = 'font-bold mb-1 text-red-500';
      }
      qaScoreEl!.textContent = `Skor: ${qaScore}`;
    });
  });

  qaNextBtn!.addEventListener('click', () => {
    currentQaIndex++;
    renderQa();
  });

  qaResetBtn!.addEventListener('click', () => {
    currentQaIndex = 0;
    qaScore = 0;
    shuffledQa.sort(() => Math.random() - 0.5);
    qaSuccess!.classList.add('hidden');
    renderQa();
  });

  renderQa();

</script>
