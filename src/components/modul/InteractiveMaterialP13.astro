---
import type { UserSessionPayload } from '../../utils/auth';

interface Props {
  user: UserSessionPayload | null;
}

const { user } = Astro.props;
---

<div class="interactive-material-p13 space-y-8" id="interactive-p13-root">

  <!-- SCENE 1: Pilih Feedback Konstruktif -->
  <section class="p-5 rounded-2xl bg-gradient-to-br from-indigo-500/10 via-blue-500/5 to-transparent border border-indigo-500/20 shadow-sm relative overflow-hidden">
    <div class="absolute top-0 right-0 w-32 h-32 bg-indigo-500/10 blur-3xl rounded-full"></div>
    
    <div class="relative z-10 space-y-4">
      <div class="flex items-center gap-2">
        <span class="p-2 rounded-lg bg-indigo-500/20 text-indigo-400 font-bold text-xs font-mono">SCENE 01</span>
        <h3 class="text-lg font-bold text-foreground">Pilih Feedback Konstruktif</h3>
      </div>
      
      <p class="text-xs text-muted-foreground leading-relaxed">
        Bandingkan respons yang membantu teman memperbaiki reviewnya dengan kritik kosong. Pilih respons di bawah ini.
      </p>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-3" id="feedback-container">
        <button type="button" class="fb-btn text-left p-4 rounded-xl border border-border bg-card hover:border-indigo-500/50 transition-all focus-visible:outline-indigo-400" data-target="apresiasi">
          <div class="font-bold text-sm mb-1 text-indigo-400">✨ Apresiasi spesifik</div>
          <p class="text-xs text-muted-foreground">“Screenshot-mu jelas menunjukkan masalah tombol.”</p>
        </button>
        <button type="button" class="fb-btn text-left p-4 rounded-xl border border-border bg-card hover:border-indigo-500/50 transition-all focus-visible:outline-indigo-400" data-target="solutif">
          <div class="font-bold text-sm mb-1 text-indigo-400">💡 Saran solutif</div>
          <p class="text-xs text-muted-foreground">“Tambahkan dampak masalah itu bagi pengguna.”</p>
        </button>
        <button type="button" class="fb-btn text-left p-4 rounded-xl border border-border bg-card hover:border-indigo-500/50 transition-all focus-visible:outline-indigo-400" data-target="fokus">
          <div class="font-bold text-sm mb-1 text-indigo-400">🎯 Fokus pada karya</div>
          <p class="text-xs text-muted-foreground">Membahas dokumen dan bukti, bukan pribadi pembuatnya.</p>
        </button>
        <button type="button" class="fb-btn text-left p-4 rounded-xl border border-border bg-card hover:border-indigo-500/50 transition-all focus-visible:outline-indigo-400" data-target="klarifikasi">
          <div class="font-bold text-sm mb-1 text-indigo-400">❓ Pertanyaan klarifikasi</div>
          <p class="text-xs text-muted-foreground">“Pengguna kategori mana yang paling terdampak?”</p>
        </button>
        <button type="button" class="fb-btn md:col-span-2 lg:col-span-1 text-left p-4 rounded-xl border border-border bg-card hover:border-indigo-500/50 transition-all focus-visible:outline-indigo-400" data-target="catatan">
          <div class="font-bold text-sm mb-1 text-indigo-400">📝 Catatan revisi</div>
          <p class="text-xs text-muted-foreground">Tuliskan poin-poin spesifik untuk ditindaklanjuti.</p>
        </button>
      </div>

      <!-- Result Display -->
      <div id="fb-display" class="hidden p-4 rounded-xl bg-indigo-500/10 border border-indigo-500/20" role="status" aria-live="polite">
        <h4 class="font-bold text-indigo-400 mb-1">Masukan Membangun!</h4>
        <p id="fb-feedback" class="text-xs text-foreground italic">...</p>
        <p class="text-xs text-muted-foreground mt-2 border-t border-indigo-500/20 pt-2">Masukan yang baik selalu spesifik, santun, dan berorientasi pada perbaikan, bukan penghakiman.</p>
      </div>
    </div>
  </section>

  <!-- SCENE 2: Susun Sandwich Feedback -->
  <section class="p-5 rounded-2xl bg-gradient-to-br from-amber-500/10 via-orange-500/5 to-transparent border border-amber-500/20 shadow-sm relative overflow-hidden">
    <div class="absolute bottom-0 right-0 w-40 h-40 bg-amber-500/10 blur-3xl rounded-full"></div>
    
    <div class="relative z-10 space-y-4">
      <div class="flex items-center gap-2">
        <span class="p-2 rounded-lg bg-amber-500/20 text-amber-500 font-bold text-xs font-mono">SCENE 02</span>
        <h3 class="text-lg font-bold text-foreground">Susun Sandwich Feedback</h3>
      </div>
      
      <p class="text-xs text-muted-foreground leading-relaxed">
        Urutkan cara memberikan masukan (feedback) dengan metode Sandwich untuk presentasi temanmu.
      </p>

      <div class="space-y-2" id="sw-container">
        <!-- Buttons will be generated by script -->
      </div>
      
      <div id="sw-success" class="hidden p-4 rounded-xl bg-green-500/10 border border-green-500/30 text-center animate-in zoom-in" role="status" aria-live="polite">
        <div class="text-3xl mb-2">🍔</div>
        <h4 class="font-bold text-green-500">Susunan Sandwich Tepat!</h4>
        <p class="text-xs text-muted-foreground mt-1">Sandwich Feedback menyeimbangkan penghargaan, area perbaikan (daging), dan dukungan semangat.</p>
      </div>
    </div>
  </section>

</div>

<script>
  // SCENE 1 Logic
  const feedbackData: Record<string, string> = {
    'apresiasi': 'Apresiasi yang baik tidak sekadar "bagus", tetapi menyebut detail mana yang memang berhasil (misal: struktur atau pilihan kata).',
    'solutif': 'Daripada menyalahkan "ini kurang lengkap", saran solutif memberikan jalan keluar apa yang bisa dikerjakan selanjutnya.',
    'fokus': 'Etika review menjaga ruang belajar tetap aman dengan memisahkan kualitas dokumen dari harga diri pembuatnya.',
    'klarifikasi': 'Pertanyaan membuka analisis lebih dalam dan merangsang presenter untuk berpikir, bukan langsung didikte.',
    'catatan': 'Masukan lisan bisa terlupakan. Feedback yang baik meninggalkan jejak tertulis agar presenter bisa merevisinya.'
  };

  const fbBtns = document.querySelectorAll('.fb-btn');
  const fbDisplay = document.getElementById('fb-display');
  const fbFeedback = document.getElementById('fb-feedback');

  fbBtns.forEach(btn => {
    btn.addEventListener('click', () => {
      const target = btn.getAttribute('data-target');
      if (target && feedbackData[target]) {
        fbBtns.forEach(b => b.classList.remove('ring-2', 'ring-indigo-500', 'bg-indigo-500/5'));
        btn.classList.add('ring-2', 'ring-indigo-500', 'bg-indigo-500/5');
        
        fbFeedback!.textContent = feedbackData[target];
        fbDisplay!.classList.remove('hidden');
      }
    });
  });

  // SCENE 2 Logic
  const swSteps = [
    { id: 'step-1', label: '1. Apresiasi Positif', detail: 'Sebutkan kekuatan presentasi atau bukti (Roti Atas).' },
    { id: 'step-2', label: '2. Kritik Solutif', detail: 'Jelaskan satu bagian yang perlu diperbaiki dan caranya (Daging).' },
    { id: 'step-3', label: '3. Dorongan Lanjut', detail: 'Tutup dengan harapan atau manfaat jika direvisi (Roti Bawah).' }
  ];

  let currentSwIndex = 0;
  const swContainer = document.getElementById('sw-container');
  const swSuccess = document.getElementById('sw-success');

  function renderSwSteps() {
    swContainer!.innerHTML = '';
    swSteps.forEach((step, index) => {
      const isLocked = index > currentSwIndex;
      const isCompleted = index < currentSwIndex;
      const isCurrent = index === currentSwIndex;
      
      const btn = document.createElement('button');
      btn.type = 'button';
      btn.className = `w-full text-left p-3 sm:p-4 rounded-xl border flex items-center justify-between transition-all focus-visible:outline-amber-400 ${
        isCompleted ? 'bg-amber-500/20 border-amber-500/50 text-foreground opacity-70' :
        isCurrent ? 'bg-amber-500 text-amber-950 font-bold border-amber-600 shadow-md transform hover:-translate-y-1' :
        'bg-secondary/30 border-border text-muted-foreground opacity-50 cursor-not-allowed'
      }`;
      
      btn.innerHTML = `
        <div class="flex items-center gap-3">
          <div class="${isCompleted ? 'text-amber-500' : isCurrent ? 'text-amber-950' : 'text-muted-foreground'} font-bold">
            ${isCompleted ? '✓' : isCurrent ? '▶' : '🔒'}
          </div>
          <div>
            <div class="text-sm ${isCurrent ? 'font-bold' : 'font-semibold'}">${step.label}</div>
            ${isCompleted || isCurrent ? `<div class="text-xs ${isCurrent ? 'text-amber-900/80' : 'text-muted-foreground'} mt-1">${step.detail}</div>` : ''}
          </div>
        </div>
      `;

      if (isCurrent) {
        btn.addEventListener('click', () => {
          currentSwIndex++;
          renderSwSteps();
          if (currentSwIndex >= swSteps.length) {
            setTimeout(() => {
              swSuccess!.classList.remove('hidden');
            }, 300);
          }
        });
      }

      swContainer!.appendChild(btn);
    });
  }

  // Initialize
  renderSwSteps();

</script>
