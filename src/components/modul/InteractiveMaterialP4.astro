---
import type { UserSessionPayload } from '../../utils/auth';

interface Props {
  user: UserSessionPayload | null;
}

const { user } = Astro.props;
---

<div class="interactive-material-p4 space-y-8" id="interactive-p4-root">

  <!-- SCENE 1: Explore Matrix Skill -->
  <section class="p-5 rounded-2xl bg-gradient-to-br from-blue-500/10 via-cyan-500/5 to-transparent border border-blue-500/20 shadow-sm relative overflow-hidden">
    <div class="absolute top-0 right-0 w-32 h-32 bg-blue-500/10 blur-3xl rounded-full"></div>
    
    <div class="relative z-10 space-y-4">
      <div class="flex items-center gap-2">
        <span class="p-2 rounded-lg bg-blue-500/20 text-blue-400 font-bold text-xs font-mono">SCENE 01</span>
        <h3 class="text-lg font-bold text-foreground">Jelajah Matriks Skill PPLG</h3>
      </div>
      
      <p class="text-xs text-muted-foreground leading-relaxed">
        Telusuri tiap kelompok skill yang menjadi bekal kariermu. Klik pada setiap kategori untuk melihat detail dan fokus pembelajaran.
      </p>

      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-3">
        <button type="button" class="matrix-btn text-left p-4 rounded-xl border border-border bg-card hover:border-blue-500/50 transition-all group focus-visible:outline-blue-400" data-target="hard-skill">
          <div class="flex items-center gap-3 mb-2">
            <span class="text-2xl group-hover:scale-110 transition-transform">💻</span>
            <span class="font-bold text-sm">Hard Skill</span>
          </div>
          <p class="text-xs text-muted-foreground line-clamp-2">Kemampuan teknis spesifik...</p>
        </button>

        <button type="button" class="matrix-btn text-left p-4 rounded-xl border border-border bg-card hover:border-blue-500/50 transition-all group focus-visible:outline-blue-400" data-target="soft-skill">
          <div class="flex items-center gap-3 mb-2">
            <span class="text-2xl group-hover:scale-110 transition-transform">🤝</span>
            <span class="font-bold text-sm">Soft Skill</span>
          </div>
          <p class="text-xs text-muted-foreground line-clamp-2">Komunikasi dan kerja tim...</p>
        </button>

        <button type="button" class="matrix-btn text-left p-4 rounded-xl border border-border bg-card hover:border-blue-500/50 transition-all group focus-visible:outline-blue-400" data-target="fase-e">
          <div class="flex items-center gap-3 mb-2">
            <span class="text-2xl group-hover:scale-110 transition-transform">🌱</span>
            <span class="font-bold text-sm">Fase E (Kelas 10)</span>
          </div>
          <p class="text-xs text-muted-foreground line-clamp-2">Fondasi awal algoritma...</p>
        </button>

        <button type="button" class="matrix-btn text-left p-4 rounded-xl border border-border bg-card hover:border-blue-500/50 transition-all group focus-visible:outline-blue-400" data-target="fase-f11">
          <div class="flex items-center gap-3 mb-2">
            <span class="text-2xl group-hover:scale-110 transition-transform">🌿</span>
            <span class="font-bold text-sm">Fase F (Kelas 11)</span>
          </div>
          <p class="text-xs text-muted-foreground line-clamp-2">Pengembangan aplikasi web...</p>
        </button>

        <button type="button" class="matrix-btn text-left p-4 rounded-xl border border-border bg-card hover:border-blue-500/50 transition-all group lg:col-span-2 focus-visible:outline-blue-400" data-target="fase-f12">
          <div class="flex items-center gap-3 mb-2">
            <span class="text-2xl group-hover:scale-110 transition-transform">🌳</span>
            <span class="font-bold text-sm">Fase F (Kelas 12)</span>
          </div>
          <p class="text-xs text-muted-foreground line-clamp-2">Proyek nyata dan persiapan industri...</p>
        </button>
      </div>

      <!-- Detail Display -->
      <div id="matrix-display" class="hidden animate-in fade-in slide-in-from-bottom-2 p-4 rounded-xl bg-blue-500/10 border border-blue-500/20" role="status" aria-live="polite">
        <h4 id="matrix-title" class="font-bold text-blue-400 mb-1">...</h4>
        <p id="matrix-desc" class="text-sm text-foreground mb-2">...</p>
        <p id="matrix-feedback" class="text-xs text-muted-foreground italic border-l-2 border-blue-500/50 pl-2">...</p>
      </div>
    </div>
  </section>

  <!-- SCENE 2: Roadmap Karier (Sequence Simulator) -->
  <section class="p-5 rounded-2xl bg-gradient-to-br from-amber-500/10 via-orange-500/5 to-transparent border border-amber-500/20 shadow-sm relative overflow-hidden">
    <div class="absolute bottom-0 right-0 w-40 h-40 bg-amber-500/10 blur-3xl rounded-full"></div>
    
    <div class="relative z-10 space-y-4">
      <div class="flex items-center gap-2">
        <span class="p-2 rounded-lg bg-amber-500/20 text-amber-500 font-bold text-xs font-mono">SCENE 02</span>
        <h3 class="text-lg font-bold text-foreground">Susun Roadmap Kariermu</h3>
      </div>
      
      <p class="text-xs text-muted-foreground leading-relaxed">
        Karier bukanlah lompatan instan. Klik tahap-tahap di bawah ini secara berurutan untuk membangun peta jalan (roadmap) dari sekolah hingga menjadi ahli.
      </p>

      <div class="space-y-2" id="roadmap-container">
        <!-- Buttons will be generated by script -->
      </div>
      
      <div id="roadmap-success" class="hidden p-4 rounded-xl bg-green-500/10 border border-green-500/30 text-center animate-in zoom-in" role="status" aria-live="polite">
        <div class="text-3xl mb-2">🏆</div>
        <h4 class="font-bold text-green-500">Roadmap Selesai!</h4>
        <p class="text-xs text-muted-foreground mt-1">Karier berkembang dari perpaduan hard skill dan soft skill yang dilatih bertahap. Tetap konsisten!</p>
      </div>
    </div>
  </section>

</div>

<script>
  // Data Scene 1
  const matrixData: Record<string, { title: string, desc: string, feedback: string }> = {
    'hard-skill': {
      title: 'Hard Skill',
      desc: 'Kemampuan teknis spesifik seperti algoritma, pemrograman web, mobile, dan basis data.',
      feedback: 'Kemampuan teknis dibuktikan lewat karya nyata (portofolio).'
    },
    'soft-skill': {
      title: 'Soft Skill',
      desc: 'Komunikasi, kerja tim, problem solving, dan manajemen waktu.',
      feedback: 'Kolaborasi memperkuat kualitas kerja teknis. Tanpa soft skill, hard skill sulit berkembang di industri.'
    },
    'fase-e': {
      title: 'Fase E (Kelas 10)',
      desc: 'Mempelajari orientasi industri PPLG, dasar algoritma, dan pemodelan perangkat lunak.',
      feedback: 'Fondasi ini menyiapkan pola pikir sistematis untuk pembelajaran berikutnya.'
    },
    'fase-f11': {
      title: 'Fase F (Kelas 11)',
      desc: 'Fokus pada pemrograman web, basis data, dan pemrograman berorientasi objek (PBO).',
      feedback: 'Kompetensi ini bertujuan untuk membangun aplikasi yang dinamis dan terstruktur.'
    },
    'fase-f12': {
      title: 'Fase F (Kelas 12)',
      desc: 'Mengerjakan pemrograman mobile, PKK (Produk Kreatif dan Kewirausahaan), dan PKL industri.',
      feedback: 'Portofolio dan pengalaman nyata di dunia kerja menjadi hal yang paling penting di fase ini.'
    }
  };

  // Logic Scene 1
  const matrixBtns = document.querySelectorAll('.matrix-btn');
  const matrixDisplay = document.getElementById('matrix-display');
  const matrixTitle = document.getElementById('matrix-title');
  const matrixDesc = document.getElementById('matrix-desc');
  const matrixFeedback = document.getElementById('matrix-feedback');

  matrixBtns.forEach(btn => {
    btn.addEventListener('click', () => {
      const target = btn.getAttribute('data-target');
      if (target && matrixData[target]) {
        matrixBtns.forEach(b => b.classList.remove('ring-2', 'ring-blue-500'));
        btn.classList.add('ring-2', 'ring-blue-500');
        
        matrixTitle!.textContent = matrixData[target].title;
        matrixDesc!.textContent = matrixData[target].desc;
        matrixFeedback!.textContent = `💡 ${matrixData[target].feedback}`;
        
        matrixDisplay!.classList.remove('hidden');
      }
    });
  });

  // Data Scene 2
  const roadmapSteps = [
    { id: 'step-1', label: '1. Fondasi Kelas 10', detail: 'Latih logika, orientasi industri, dan pemodelan dasar.' },
    { id: 'step-2', label: '2. Pendalaman Kelas 11', detail: 'Bangun web, API, basis data, dan arsitektur kode.' },
    { id: 'step-3', label: '3. Portofolio Kelas 12', detail: 'Kerjakan aplikasi mobile, proyek PKK, dan terjun PKL.' },
    { id: 'step-4', label: '4. Intern / Junior Engineer', detail: 'Belajar dalam tugas nyata industri dengan bimbingan mentor.' },
    { id: 'step-5', label: '5. Mid / Senior / Lead', detail: 'Menyelesaikan masalah kompleks arsitektur dan membimbing tim.' }
  ];

  let currentRoadmapIndex = 0;
  const roadmapContainer = document.getElementById('roadmap-container');
  const roadmapSuccess = document.getElementById('roadmap-success');

  function renderRoadmap() {
    roadmapContainer!.innerHTML = '';
    roadmapSteps.forEach((step, index) => {
      const isLocked = index > currentRoadmapIndex;
      const isCompleted = index < currentRoadmapIndex;
      const isCurrent = index === currentRoadmapIndex;
      
      const btn = document.createElement('button');
      btn.type = 'button';
      btn.className = `w-full text-left p-3 sm:p-4 rounded-xl border flex items-center justify-between transition-all focus-visible:outline-amber-400 ${
        isCompleted ? 'bg-amber-500/20 border-amber-500/50 text-foreground opacity-70' :
        isCurrent ? 'bg-amber-500 text-amber-950 font-bold border-amber-600 shadow-md transform hover:-translate-y-1' :
        'bg-secondary/30 border-border text-muted-foreground opacity-50 cursor-not-allowed'
      }`;
      
      btn.innerHTML = `
        <div class="flex items-center gap-3">
          <div class="${isCompleted ? 'text-amber-500' : isCurrent ? 'text-amber-950' : 'text-muted-foreground'} font-bold">
            ${isCompleted ? '✓' : isCurrent ? '▶' : '🔒'}
          </div>
          <div>
            <div class="text-sm ${isCurrent ? 'font-bold' : 'font-semibold'}">${step.label}</div>
            ${isCompleted || isCurrent ? `<div class="text-xs ${isCurrent ? 'text-amber-900/80' : 'text-muted-foreground'} mt-1">${step.detail}</div>` : ''}
          </div>
        </div>
      `;

      if (isCurrent) {
        btn.addEventListener('click', () => {
          currentRoadmapIndex++;
          renderRoadmap();
          if (currentRoadmapIndex >= roadmapSteps.length) {
            setTimeout(() => {
              roadmapSuccess!.classList.remove('hidden');
            }, 300);
          }
        });
      }

      roadmapContainer!.appendChild(btn);
    });
  }

  // Initialize
  renderRoadmap();

</script>
