---
import type { UserSessionPayload } from '../../utils/auth';

interface Props {
  user: UserSessionPayload | null;
}

const { user } = Astro.props;
---

<div class="interactive-material-p14 space-y-8" id="interactive-p14-root">

  <!-- SCENE 1: Cek Standar Dokumen (Checklist) -->
  <section class="p-5 rounded-2xl bg-gradient-to-br from-indigo-500/10 via-blue-500/5 to-transparent border border-indigo-500/20 shadow-sm relative overflow-hidden">
    <div class="absolute top-0 right-0 w-32 h-32 bg-indigo-500/10 blur-3xl rounded-full"></div>
    
    <div class="relative z-10 space-y-4">
      <div class="flex items-center gap-2">
        <span class="p-2 rounded-lg bg-indigo-500/20 text-indigo-400 font-bold text-xs font-mono">SCENE 01</span>
        <h3 class="text-lg font-bold text-foreground">Cek Standar Dokumen Review</h3>
      </div>
      
      <p class="text-xs text-muted-foreground leading-relaxed">
        Pastikan laporan final memenuhi struktur dan keterbacaan. Dokumen standar industri memadukan struktur, bukti, dan rekomendasi.
      </p>

      <div class="space-y-3" id="doc-checklist">
        <!-- Checklist items -->
        <label class="flex items-start gap-3 p-4 rounded-xl border border-border bg-card hover:bg-indigo-500/5 cursor-pointer transition-all group">
          <input type="checkbox" class="doc-cb mt-1 h-5 w-5 rounded border-border text-indigo-600 focus:ring-indigo-500" data-index="0" />
          <div>
            <div class="font-bold text-sm group-hover:text-indigo-400 transition-colors">Cover Resmi</div>
            <p class="text-xs text-muted-foreground mt-1">Buat halaman awal dengan identitas dokumen. Cover memberi konteks profesional.</p>
          </div>
        </label>
        
        <label class="flex items-start gap-3 p-4 rounded-xl border border-border bg-card hover:bg-indigo-500/5 cursor-pointer transition-all group">
          <input type="checkbox" class="doc-cb mt-1 h-5 w-5 rounded border-border text-indigo-600 focus:ring-indigo-500" data-index="1" />
          <div>
            <div class="font-bold text-sm group-hover:text-indigo-400 transition-colors">Enam Komponen</div>
            <p class="text-xs text-muted-foreground mt-1">Periksa seluruh bagian review telah terisi. Tidak boleh ada komponen yang terlewat.</p>
          </div>
        </label>

        <label class="flex items-start gap-3 p-4 rounded-xl border border-border bg-card hover:bg-indigo-500/5 cursor-pointer transition-all group">
          <input type="checkbox" class="doc-cb mt-1 h-5 w-5 rounded border-border text-indigo-600 focus:ring-indigo-500" data-index="2" />
          <div>
            <div class="font-bold text-sm group-hover:text-indigo-400 transition-colors">Screenshot Anotasi</div>
            <p class="text-xs text-muted-foreground mt-1">Gunakan bukti visual tajam dan berlabel. Visual memperkuat analisis UI/UX.</p>
          </div>
        </label>

        <label class="flex items-start gap-3 p-4 rounded-xl border border-border bg-card hover:bg-indigo-500/5 cursor-pointer transition-all group">
          <input type="checkbox" class="doc-cb mt-1 h-5 w-5 rounded border-border text-indigo-600 focus:ring-indigo-500" data-index="3" />
          <div>
            <div class="font-bold text-sm group-hover:text-indigo-400 transition-colors">Tipografi Rapi</div>
            <p class="text-xs text-muted-foreground mt-1">Pakai judul dan isi yang konsisten. Keterbacaan memudahkan penilai.</p>
          </div>
        </label>

        <label class="flex items-start gap-3 p-4 rounded-xl border border-border bg-card hover:bg-indigo-500/5 cursor-pointer transition-all group">
          <input type="checkbox" class="doc-cb mt-1 h-5 w-5 rounded border-border text-indigo-600 focus:ring-indigo-500" data-index="4" />
          <div>
            <div class="font-bold text-sm group-hover:text-indigo-400 transition-colors">Rekomendasi Solutif</div>
            <p class="text-xs text-muted-foreground mt-1">Tuliskan saran konkret untuk tim developer. Rekomendasi adalah penutup yang dapat ditindaklanjuti.</p>
          </div>
        </label>
      </div>

      <!-- Feedback Area -->
      <div id="doc-progress-container" class="mt-4">
        <div class="flex justify-between text-xs font-medium text-muted-foreground mb-2">
          <span>Kesiapan Dokumen</span>
          <span id="doc-percent">0%</span>
        </div>
        <div class="w-full bg-secondary h-2.5 rounded-full overflow-hidden">
          <div id="doc-bar" class="bg-indigo-500 h-2.5 rounded-full transition-all duration-500" style="width: 0%"></div>
        </div>
      </div>

      <div id="doc-success" class="hidden mt-4 p-4 rounded-xl bg-indigo-500/10 border border-indigo-500/30 text-center animate-in fade-in" role="status" aria-live="polite">
        <h4 class="font-bold text-indigo-400">Dokumen Memenuhi Standar! 📄</h4>
        <p class="text-xs text-muted-foreground mt-1">Dokumen standar industri memadukan struktur, bukti, dan rekomendasi.</p>
      </div>
    </div>
  </section>

  <!-- SCENE 2: Pilih Aksi Kualitas (Scenario) -->
  <section class="p-5 rounded-2xl bg-gradient-to-br from-emerald-500/10 via-teal-500/5 to-transparent border border-emerald-500/20 shadow-sm relative overflow-hidden">
    <div class="absolute bottom-0 right-0 w-40 h-40 bg-emerald-500/10 blur-3xl rounded-full"></div>
    
    <div class="relative z-10 space-y-4">
      <div class="flex items-center gap-2">
        <span class="p-2 rounded-lg bg-emerald-500/20 text-emerald-500 font-bold text-xs font-mono">SCENE 02</span>
        <h3 class="text-lg font-bold text-foreground">Pilih Aksi Kualitas</h3>
      </div>
      
      <p class="text-xs text-muted-foreground leading-relaxed">
        Tentukan perbaikan yang paling tepat dari kondisi dokumen di bawah ini.
      </p>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-3" id="aksi-container">
        <!-- Rute Options -->
        <button type="button" class="aksi-btn text-left p-4 rounded-xl border border-border bg-card hover:border-emerald-500/50 transition-all focus-visible:outline-emerald-400" data-target="kosong">
          <div class="font-bold text-sm mb-1 text-emerald-600 dark:text-emerald-400">❓ Komponen Kosong</div>
          <p class="text-xs text-muted-foreground">Bagian target pengguna belum ditulis.</p>
        </button>
        <button type="button" class="aksi-btn text-left p-4 rounded-xl border border-border bg-card hover:border-emerald-500/50 transition-all focus-visible:outline-emerald-400" data-target="buram">
          <div class="font-bold text-sm mb-1 text-emerald-600 dark:text-emerald-400">🌫️ Screenshot Buram</div>
          <p class="text-xs text-muted-foreground">Bukti visual sulit dibaca saat diperbesar.</p>
        </button>
        <button type="button" class="aksi-btn text-left p-4 rounded-xl border border-border bg-card hover:border-emerald-500/50 transition-all focus-visible:outline-emerald-400" data-target="umum">
          <div class="font-bold text-sm mb-1 text-emerald-600 dark:text-emerald-400">💬 Saran Umum</div>
          <p class="text-xs text-muted-foreground">Rekomendasi hanya berbunyi “tingkatkan lagi”.</p>
        </button>
        <button type="button" class="aksi-btn text-left p-4 rounded-xl border border-border bg-card hover:border-emerald-500/50 transition-all focus-visible:outline-emerald-400" data-target="format">
          <div class="font-bold text-sm mb-1 text-emerald-600 dark:text-emerald-400">📏 Format Tidak Konsisten</div>
          <p class="text-xs text-muted-foreground">Judul dan ukuran huruf berubah-ubah.</p>
        </button>
        <button type="button" class="aksi-btn md:col-span-2 text-left p-4 rounded-xl border border-border bg-card hover:border-emerald-500/50 transition-all focus-visible:outline-emerald-400" data-target="teman">
          <div class="font-bold text-sm mb-1 text-emerald-600 dark:text-emerald-400">🤝 Masukan Teman Belum Diterapkan</div>
          <p class="text-xs text-muted-foreground">Catatan peer feedback masih relevan.</p>
        </button>
      </div>

      <!-- Result Display -->
      <div id="aksi-display" class="hidden p-4 rounded-xl bg-emerald-500/10 border border-emerald-500/20" role="status" aria-live="polite">
        <h4 class="font-bold text-emerald-600 dark:text-emerald-400 mb-1">Aksi Perbaikan Tepat!</h4>
        <p id="aksi-feedback" class="text-xs text-foreground italic">...</p>
        <p class="text-xs text-muted-foreground mt-2 border-t border-emerald-500/20 pt-2">Prioritaskan perbaikan yang meningkatkan kelengkapan dan keterbacaan evidence.</p>
      </div>
    </div>
  </section>

</div>

<script>
  // SCENE 1 Logic (Checklist)
  const docCheckboxes = document.querySelectorAll<HTMLInputElement>('.doc-cb');
  const docBar = document.getElementById('doc-bar');
  const docPercent = document.getElementById('doc-percent');
  const docSuccess = document.getElementById('doc-success');

  function updateDocProgress() {
    const total = docCheckboxes.length;
    const checked = Array.from(docCheckboxes).filter(cb => cb.checked).length;
    const percentage = Math.round((checked / total) * 100);

    docBar!.style.width = `${percentage}%`;
    docPercent!.textContent = `${percentage}%`;

    if (percentage === 100) {
      docSuccess!.classList.remove('hidden');
    } else {
      docSuccess!.classList.add('hidden');
    }
  }

  docCheckboxes.forEach(cb => {
    cb.addEventListener('change', updateDocProgress);
  });

  // SCENE 2 Logic
  const aksiData: Record<string, string> = {
    'kosong': 'Solusi: Lengkapi komponen tersebut terlebih dahulu sebelum mempercantik tampilan desain dokumen.',
    'buram': 'Solusi: Ganti dengan tangkapan layar (screenshot) asli (native) yang resolusinya lebih jelas.',
    'umum': 'Solusi: Ubah menjadi tindakan perbaikan teknis atau UX yang spesifik, misal: "Perbesar font ke 16px".',
    'format': 'Solusi: Terapkan gaya (style) heading dan paragraf dokumen yang seragam dan rapi.',
    'teman': 'Solusi: Tindak lanjuti catatan feedback tersebut, perbaiki dokumen, lalu baru ekspor ke PDF.'
  };

  const aksiBtns = document.querySelectorAll('.aksi-btn');
  const aksiDisplay = document.getElementById('aksi-display');
  const aksiFeedback = document.getElementById('aksi-feedback');

  aksiBtns.forEach(btn => {
    btn.addEventListener('click', () => {
      const target = btn.getAttribute('data-target');
      if (target && aksiData[target]) {
        aksiBtns.forEach(b => b.classList.remove('ring-2', 'ring-emerald-500', 'bg-emerald-500/5'));
        btn.classList.add('ring-2', 'ring-emerald-500', 'bg-emerald-500/5');
        
        aksiFeedback!.textContent = aksiData[target];
        aksiDisplay!.classList.remove('hidden');
      }
    });
  });

</script>
