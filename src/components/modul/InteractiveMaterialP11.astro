---
import type { UserSessionPayload } from '../../utils/auth';

interface Props {
  user: UserSessionPayload | null;
}

const { user } = Astro.props;
---

<div class="interactive-material-p11 space-y-8" id="interactive-p11-root">

  <!-- SCENE 1: Explore 6 Komponen Review -->
  <section class="p-5 rounded-2xl bg-gradient-to-br from-purple-500/10 via-fuchsia-500/5 to-transparent border border-purple-500/20 shadow-sm relative overflow-hidden">
    <div class="absolute top-0 right-0 w-32 h-32 bg-purple-500/10 blur-3xl rounded-full"></div>
    
    <div class="relative z-10 space-y-4">
      <div class="flex items-center gap-2">
        <span class="p-2 rounded-lg bg-purple-500/20 text-purple-400 font-bold text-xs font-mono">SCENE 01</span>
        <h3 class="text-lg font-bold text-foreground">Jelajah 6 Komponen Review</h3>
      </div>
      
      <p class="text-xs text-muted-foreground leading-relaxed">
        Kenali peran setiap bagian dalam dokumen review. Enam komponen ini membuat review lengkap dan mudah diikuti.
      </p>

      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-3">
        <button type="button" class="komp-btn text-left p-4 rounded-xl border border-border bg-card hover:border-purple-500/50 transition-all group focus-visible:outline-purple-400" data-target="identitas">
          <div class="flex items-center gap-3 mb-2">
            <span class="text-2xl group-hover:scale-110 transition-transform">🏷️</span>
            <span class="font-bold text-sm">Identitas Produk</span>
          </div>
          <p class="text-xs text-muted-foreground line-clamp-1">Nama, kategori, dan konteks aplikasi.</p>
        </button>

        <button type="button" class="komp-btn text-left p-4 rounded-xl border border-border bg-card hover:border-purple-500/50 transition-all group focus-visible:outline-purple-400" data-target="target">
          <div class="flex items-center gap-3 mb-2">
            <span class="text-2xl group-hover:scale-110 transition-transform">🎯</span>
            <span class="font-bold text-sm">Target Pengguna</span>
          </div>
          <p class="text-xs text-muted-foreground line-clamp-1">Siapa pengguna utama dan kebutuhannya.</p>
        </button>

        <button type="button" class="komp-btn text-left p-4 rounded-xl border border-border bg-card hover:border-purple-500/50 transition-all group focus-visible:outline-purple-400" data-target="fungsi">
          <div class="flex items-center gap-3 mb-2">
            <span class="text-2xl group-hover:scale-110 transition-transform">⚙️</span>
            <span class="font-bold text-sm">Fungsi Utama</span>
          </div>
          <p class="text-xs text-muted-foreground line-clamp-1">Layanan inti dan alur kerjanya.</p>
        </button>

        <button type="button" class="komp-btn text-left p-4 rounded-xl border border-border bg-card hover:border-purple-500/50 transition-all group focus-visible:outline-purple-400" data-target="uiux">
          <div class="flex items-center gap-3 mb-2">
            <span class="text-2xl group-hover:scale-110 transition-transform">📱</span>
            <span class="font-bold text-sm">UI/UX</span>
          </div>
          <p class="text-xs text-muted-foreground line-clamp-1">Tampilan dan pengalaman pengguna.</p>
        </button>

        <button type="button" class="komp-btn text-left p-4 rounded-xl border border-border bg-card hover:border-purple-500/50 transition-all group focus-visible:outline-purple-400" data-target="plusminus">
          <div class="flex items-center gap-3 mb-2">
            <span class="text-2xl group-hover:scale-110 transition-transform">⚖️</span>
            <span class="font-bold text-sm">Kelebihan-Kekurangan</span>
          </div>
          <p class="text-xs text-muted-foreground line-clamp-1">Hal yang bekerja baik dan perlu diperbaiki.</p>
        </button>
        
        <button type="button" class="komp-btn text-left p-4 rounded-xl border border-border bg-card hover:border-purple-500/50 transition-all group focus-visible:outline-purple-400" data-target="saran">
          <div class="flex items-center gap-3 mb-2">
            <span class="text-2xl group-hover:scale-110 transition-transform">💡</span>
            <span class="font-bold text-sm">Rekomendasi</span>
          </div>
          <p class="text-xs text-muted-foreground line-clamp-1">Saran solutif bagi tim pengembang.</p>
        </button>
      </div>

      <!-- Result Display -->
      <div id="komp-display" class="hidden animate-in fade-in slide-in-from-bottom-2 p-4 rounded-xl bg-purple-500/10 border border-purple-500/20" role="status" aria-live="polite">
        <h4 id="komp-title" class="font-bold text-purple-400 mb-1">...</h4>
        <p id="komp-desc" class="text-sm text-foreground mb-2">...</p>
        <p id="komp-feedback" class="text-xs text-muted-foreground italic border-l-2 border-purple-500/50 pl-2">...</p>
      </div>
    </div>
  </section>

  <!-- SCENE 2: Susun Prioritas Review (Sequence Simulator) -->
  <section class="p-5 rounded-2xl bg-gradient-to-br from-amber-500/10 via-orange-500/5 to-transparent border border-amber-500/20 shadow-sm relative overflow-hidden">
    <div class="absolute bottom-0 right-0 w-40 h-40 bg-amber-500/10 blur-3xl rounded-full"></div>
    
    <div class="relative z-10 space-y-4">
      <div class="flex items-center gap-2">
        <span class="p-2 rounded-lg bg-amber-500/20 text-amber-500 font-bold text-xs font-mono">SCENE 02</span>
        <h3 class="text-lg font-bold text-foreground">Susun Prioritas Review</h3>
      </div>
      
      <p class="text-xs text-muted-foreground leading-relaxed">
        Urutkan langkah kerja sebelum menulis draf dokumen review agar tidak ada informasi yang terlewat.
      </p>

      <div class="space-y-2" id="prio-container">
        <!-- Buttons will be generated by script -->
      </div>
      
      <div id="prio-success" class="hidden p-4 rounded-xl bg-green-500/10 border border-green-500/30 text-center animate-in zoom-in" role="status" aria-live="polite">
        <div class="text-3xl mb-2">📋</div>
        <h4 class="font-bold text-green-500">Urutan Prioritas Tepat!</h4>
        <p class="text-xs text-muted-foreground mt-1">Prioritas yang benar memastikan analisis dibangun dari pengamatan, bukan asumsi.</p>
      </div>
    </div>
  </section>

</div>

<script>
  // SCENE 1 Logic
  const kompData: Record<string, { title: string, desc: string, feedback: string }> = {
    'identitas': {
      title: 'Identitas Produk',
      desc: 'Nama, kategori, dan konteks aplikasi.',
      feedback: 'Pembaca perlu tahu objek review secara detail dan jelas.'
    },
    'target': {
      title: 'Target Pengguna',
      desc: 'Siapa pengguna utama dan kebutuhannya.',
      feedback: 'Target pengguna membantu menilai kecocokan fitur produk.'
    },
    'fungsi': {
      title: 'Fungsi Utama',
      desc: 'Layanan inti dan alur kerjanya.',
      feedback: 'Fungsi menjelaskan nilai nyata produk bagi pengguna.'
    },
    'uiux': {
      title: 'UI/UX',
      desc: 'Tampilan dan pengalaman pengguna.',
      feedback: 'Gunakan observasi visual dan interaksi yang spesifik.'
    },
    'plusminus': {
      title: 'Kelebihan-Kekurangan',
      desc: 'Hal yang bekerja baik dan perlu diperbaiki.',
      feedback: 'Seimbangkan apresiasi fitur bagus dengan kritik fitur bermasalah.'
    },
    'saran': {
      title: 'Rekomendasi',
      desc: 'Saran solutif bagi tim pengembang.',
      feedback: 'Saran harus dapat ditindaklanjuti secara realistis oleh developer.'
    }
  };

  const kompBtns = document.querySelectorAll('.komp-btn');
  const kompDisplay = document.getElementById('komp-display');
  const kompTitle = document.getElementById('komp-title');
  const kompDesc = document.getElementById('komp-desc');
  const kompFeedback = document.getElementById('komp-feedback');

  kompBtns.forEach(btn => {
    btn.addEventListener('click', () => {
      const target = btn.getAttribute('data-target');
      if (target && kompData[target]) {
        kompBtns.forEach(b => b.classList.remove('ring-2', 'ring-purple-500'));
        btn.classList.add('ring-2', 'ring-purple-500');
        
        kompTitle!.textContent = kompData[target].title;
        kompDesc!.textContent = kompData[target].desc;
        kompFeedback!.textContent = `💡 ${kompData[target].feedback}`;
        
        kompDisplay!.classList.remove('hidden');
      }
    });
  });

  // SCENE 2 Logic
  const prioSteps = [
    { id: 'step-1', label: '1. Pilih satu aplikasi', detail: 'Tetapkan objek yang akan dikaji.' },
    { id: 'step-2', label: '2. Catat identitas', detail: 'Tulis nama, kategori, dan konteks produk.' },
    { id: 'step-3', label: '3. Kenali pengguna', detail: 'Tentukan target pengguna dan kebutuhannya.' },
    { id: 'step-4', label: '4. Petakan fungsi', detail: 'Telusuri fitur dan alur utama aplikasi.' },
    { id: 'step-5', label: '5. Kumpulkan bukti', detail: 'Siapkan screenshot dan catatan temuan.' }
  ];

  let currentPrioIndex = 0;
  const prioContainer = document.getElementById('prio-container');
  const prioSuccess = document.getElementById('prio-success');

  function renderPrioSteps() {
    prioContainer!.innerHTML = '';
    prioSteps.forEach((step, index) => {
      const isLocked = index > currentPrioIndex;
      const isCompleted = index < currentPrioIndex;
      const isCurrent = index === currentPrioIndex;
      
      const btn = document.createElement('button');
      btn.type = 'button';
      btn.className = `w-full text-left p-3 sm:p-4 rounded-xl border flex items-center justify-between transition-all focus-visible:outline-amber-400 ${
        isCompleted ? 'bg-amber-500/20 border-amber-500/50 text-foreground opacity-70' :
        isCurrent ? 'bg-amber-500 text-amber-950 font-bold border-amber-600 shadow-md transform hover:-translate-y-1' :
        'bg-secondary/30 border-border text-muted-foreground opacity-50 cursor-not-allowed'
      }`;
      
      btn.innerHTML = `
        <div class="flex items-center gap-3">
          <div class="${isCompleted ? 'text-amber-500' : isCurrent ? 'text-amber-950' : 'text-muted-foreground'} font-bold">
            ${isCompleted ? '✓' : isCurrent ? '▶' : '🔒'}
          </div>
          <div>
            <div class="text-sm ${isCurrent ? 'font-bold' : 'font-semibold'}">${step.label}</div>
            ${isCompleted || isCurrent ? `<div class="text-xs ${isCurrent ? 'text-amber-900/80' : 'text-muted-foreground'} mt-1">${step.detail}</div>` : ''}
          </div>
        </div>
      `;

      if (isCurrent) {
        btn.addEventListener('click', () => {
          currentPrioIndex++;
          renderPrioSteps();
          if (currentPrioIndex >= prioSteps.length) {
            setTimeout(() => {
              prioSuccess!.classList.remove('hidden');
            }, 300);
          }
        });
      }

      prioContainer!.appendChild(btn);
    });
  }

  // Initialize
  renderPrioSteps();

</script>
