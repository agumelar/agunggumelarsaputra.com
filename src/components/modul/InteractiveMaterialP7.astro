---
import type { UserSessionPayload } from '../../utils/auth';

interface Props {
  user: UserSessionPayload | null;
}

const { user } = Astro.props;
---

<div class="interactive-material-p7 space-y-8" id="interactive-p7-root">

  <!-- SCENE 1: Explore Mind Map Branches -->
  <section class="p-5 rounded-2xl bg-gradient-to-br from-indigo-500/10 via-blue-500/5 to-transparent border border-indigo-500/20 shadow-sm relative overflow-hidden">
    <div class="absolute top-0 right-0 w-32 h-32 bg-indigo-500/10 blur-3xl rounded-full"></div>
    
    <div class="relative z-10 space-y-4">
      <div class="flex items-center gap-2">
        <span class="p-2 rounded-lg bg-indigo-500/20 text-indigo-400 font-bold text-xs font-mono">SCENE 01</span>
        <h3 class="text-lg font-bold text-foreground">Jelajah Cabang Mind Map</h3>
      </div>
      
      <p class="text-xs text-muted-foreground leading-relaxed">
        Kenali elemen-elemen penting yang membuat peta profesi informatif. Klik pada cabang di bawah untuk melihat detailnya.
      </p>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-3">
        <button type="button" class="branch-btn text-left p-4 rounded-xl border border-border bg-card hover:border-indigo-500/50 transition-all group focus-visible:outline-indigo-400" data-target="pusat">
          <div class="flex items-center gap-3 mb-2">
            <span class="text-2xl group-hover:scale-110 transition-transform">🎯</span>
            <span class="font-bold text-sm">Profesi Pusat</span>
          </div>
          <p class="text-xs text-muted-foreground line-clamp-1">Topik utama: Profesi PPLG.</p>
        </button>

        <button type="button" class="branch-btn text-left p-4 rounded-xl border border-border bg-card hover:border-indigo-500/50 transition-all group focus-visible:outline-indigo-400" data-target="tugas">
          <div class="flex items-center gap-3 mb-2">
            <span class="text-2xl group-hover:scale-110 transition-transform">📝</span>
            <span class="font-bold text-sm">Tugas Utama</span>
          </div>
          <p class="text-xs text-muted-foreground line-clamp-1">Ringkas tanggung jawab tiap profesi.</p>
        </button>

        <button type="button" class="branch-btn text-left p-4 rounded-xl border border-border bg-card hover:border-indigo-500/50 transition-all group focus-visible:outline-indigo-400" data-target="tools">
          <div class="flex items-center gap-3 mb-2">
            <span class="text-2xl group-hover:scale-110 transition-transform">🛠️</span>
            <span class="font-bold text-sm">Tools</span>
          </div>
          <p class="text-xs text-muted-foreground line-clamp-1">Software atau bahasa yang dipakai.</p>
        </button>

        <button type="button" class="branch-btn text-left p-4 rounded-xl border border-border bg-card hover:border-indigo-500/50 transition-all group focus-visible:outline-indigo-400" data-target="karya">
          <div class="flex items-center gap-3 mb-2">
            <span class="text-2xl group-hover:scale-110 transition-transform">✨</span>
            <span class="font-bold text-sm">Hasil Karya</span>
          </div>
          <p class="text-xs text-muted-foreground line-clamp-1">Tampilkan contoh produk nyata.</p>
        </button>

        <button type="button" class="branch-btn text-left p-4 rounded-xl border border-border bg-card hover:border-indigo-500/50 transition-all group lg:col-span-2 focus-visible:outline-indigo-400" data-target="minat">
          <div class="flex items-center gap-3 mb-2">
            <span class="text-2xl group-hover:scale-110 transition-transform">⭐</span>
            <span class="font-bold text-sm">Minat Pribadi</span>
          </div>
          <p class="text-xs text-muted-foreground line-clamp-1">Tandai profesi yang ingin dieksplorasi.</p>
        </button>
      </div>

      <!-- Result Display -->
      <div id="branch-display" class="hidden animate-in fade-in slide-in-from-bottom-2 p-4 rounded-xl bg-indigo-500/10 border border-indigo-500/20" role="status" aria-live="polite">
        <h4 id="branch-title" class="font-bold text-indigo-400 mb-1">...</h4>
        <p id="branch-desc" class="text-sm text-foreground mb-2">...</p>
        <p id="branch-feedback" class="text-xs text-muted-foreground italic border-l-2 border-indigo-500/50 pl-2">...</p>
      </div>
    </div>
  </section>

  <!-- SCENE 2: Hubungan Node Checklist -->
  <section class="p-5 rounded-2xl bg-gradient-to-br from-teal-500/10 via-emerald-500/5 to-transparent border border-teal-500/20 shadow-sm relative overflow-hidden">
    <div class="absolute bottom-0 right-0 w-40 h-40 bg-teal-500/10 blur-3xl rounded-full"></div>
    
    <div class="relative z-10 space-y-4">
      <div class="flex items-center gap-2">
        <span class="p-2 rounded-lg bg-teal-500/20 text-teal-500 font-bold text-xs font-mono">SCENE 02</span>
        <h3 class="text-lg font-bold text-foreground">Cek Hubungan Node</h3>
      </div>
      
      <p class="text-xs text-muted-foreground leading-relaxed">
        Periksa keterbacaan hubungan antaride pada mind map. Mind map yang baik memakai hierarki dan kata kunci yang mudah dipindai.
      </p>

      <div class="space-y-3" id="node-checklist">
        <!-- Checklist items -->
        <label class="flex items-start gap-3 p-4 rounded-xl border border-border bg-card hover:bg-teal-500/5 cursor-pointer transition-all group">
          <input type="checkbox" class="node-cb mt-1 h-5 w-5 rounded border-border text-teal-600 focus:ring-teal-500" data-index="0" />
          <div>
            <div class="font-bold text-sm group-hover:text-teal-600 dark:group-hover:text-teal-400 transition-colors">Garis Penghubung</div>
            <p class="text-xs text-muted-foreground mt-1">Setiap cabang terhubung ke topik yang tepat. Garis menunjukkan hubungan ide.</p>
          </div>
        </label>
        
        <label class="flex items-start gap-3 p-4 rounded-xl border border-border bg-card hover:bg-teal-500/5 cursor-pointer transition-all group">
          <input type="checkbox" class="node-cb mt-1 h-5 w-5 rounded border-border text-teal-600 focus:ring-teal-500" data-index="1" />
          <div>
            <div class="font-bold text-sm group-hover:text-teal-600 dark:group-hover:text-teal-400 transition-colors">Hierarki Jelas</div>
            <p class="text-xs text-muted-foreground mt-1">Node umum berada dekat pusat, rincian di luar. Hierarki mencegah peta menjadi datar.</p>
          </div>
        </label>

        <label class="flex items-start gap-3 p-4 rounded-xl border border-border bg-card hover:bg-teal-500/5 cursor-pointer transition-all group">
          <input type="checkbox" class="node-cb mt-1 h-5 w-5 rounded border-border text-teal-600 focus:ring-teal-500" data-index="2" />
          <div>
            <div class="font-bold text-sm group-hover:text-teal-600 dark:group-hover:text-teal-400 transition-colors">Warna Pembeda</div>
            <p class="text-xs text-muted-foreground mt-1">Gunakan warna konsisten untuk kategori. Warna membantu navigasi visual.</p>
          </div>
        </label>

        <label class="flex items-start gap-3 p-4 rounded-xl border border-border bg-card hover:bg-teal-500/5 cursor-pointer transition-all group">
          <input type="checkbox" class="node-cb mt-1 h-5 w-5 rounded border-border text-teal-600 focus:ring-teal-500" data-index="3" />
          <div>
            <div class="font-bold text-sm group-hover:text-teal-600 dark:group-hover:text-teal-400 transition-colors">Label Ringkas</div>
            <p class="text-xs text-muted-foreground mt-1">Pakai frasa singkat yang mudah dibaca. Label ringkas menjaga peta tidak padat.</p>
          </div>
        </label>

        <label class="flex items-start gap-3 p-4 rounded-xl border border-border bg-card hover:bg-teal-500/5 cursor-pointer transition-all group">
          <input type="checkbox" class="node-cb mt-1 h-5 w-5 rounded border-border text-teal-600 focus:ring-teal-500" data-index="4" />
          <div>
            <div class="font-bold text-sm group-hover:text-teal-600 dark:group-hover:text-teal-400 transition-colors">Keterkaitan Profesi</div>
            <p class="text-xs text-muted-foreground mt-1">Hubungkan peran yang berkolaborasi. Kolaborasi memperlihatkan ekosistem kerja.</p>
          </div>
        </label>
      </div>

      <!-- Feedback Area -->
      <div id="node-progress-container" class="mt-4">
        <div class="flex justify-between text-xs font-medium text-muted-foreground mb-2">
          <span>Kesiapan Hubungan Node</span>
          <span id="node-percent">0%</span>
        </div>
        <div class="w-full bg-secondary h-2.5 rounded-full overflow-hidden">
          <div id="node-bar" class="bg-teal-500 h-2.5 rounded-full transition-all duration-500" style="width: 0%"></div>
        </div>
      </div>

      <div id="node-success" class="hidden mt-4 p-4 rounded-xl bg-teal-500/10 border border-teal-500/30 text-center animate-in fade-in" role="status" aria-live="polite">
        <h4 class="font-bold text-teal-500">Mind Map Tervalidasi! 🗺️</h4>
        <p class="text-xs text-muted-foreground mt-1">Hubungan yang rapi membuat pembaca memahami peta tanpa penjelasan panjang.</p>
      </div>
    </div>
  </section>

</div>

<script>
  // SCENE 1 Logic
  const branchData: Record<string, { title: string, desc: string, feedback: string }> = {
    'pusat': {
      title: 'Profesi Pusat',
      desc: 'Topik utama: Profesi PPLG.',
      feedback: 'Pusat menjaga seluruh cabang tetap fokus.'
    },
    'tugas': {
      title: 'Tugas Utama',
      desc: 'Ringkas tanggung jawab tiap profesi.',
      feedback: 'Gunakan kata kunci, bukan paragraf panjang.'
    },
    'tools': {
      title: 'Tools',
      desc: 'Catat software atau bahasa yang dipakai.',
      feedback: 'Tools memperjelas kebutuhan kompetensi.'
    },
    'karya': {
      title: 'Hasil Karya',
      desc: 'Tampilkan contoh produk nyata.',
      feedback: 'Contoh menghubungkan peran dengan dampak.'
    },
    'minat': {
      title: 'Minat Pribadi',
      desc: 'Tandai profesi yang ingin dieksplorasi.',
      feedback: 'Penandaan membantu refleksi karier.'
    }
  };

  const branchBtns = document.querySelectorAll('.branch-btn');
  const branchDisplay = document.getElementById('branch-display');
  const branchTitle = document.getElementById('branch-title');
  const branchDesc = document.getElementById('branch-desc');
  const branchFeedback = document.getElementById('branch-feedback');

  branchBtns.forEach(btn => {
    btn.addEventListener('click', () => {
      const target = btn.getAttribute('data-target');
      if (target && branchData[target]) {
        branchBtns.forEach(b => b.classList.remove('ring-2', 'ring-indigo-500', 'bg-indigo-500/5'));
        btn.classList.add('ring-2', 'ring-indigo-500', 'bg-indigo-500/5');
        
        branchTitle!.textContent = branchData[target].title;
        branchDesc!.textContent = branchData[target].desc;
        branchFeedback!.textContent = `💡 ${branchData[target].feedback}`;
        
        branchDisplay!.classList.remove('hidden');
      }
    });
  });

  // SCENE 2 Logic (Checklist)
  const nodeCheckboxes = document.querySelectorAll<HTMLInputElement>('.node-cb');
  const nodeBar = document.getElementById('node-bar');
  const nodePercent = document.getElementById('node-percent');
  const nodeSuccess = document.getElementById('node-success');

  function updateNodeProgress() {
    const total = nodeCheckboxes.length;
    const checked = Array.from(nodeCheckboxes).filter(cb => cb.checked).length;
    const percentage = Math.round((checked / total) * 100);

    nodeBar!.style.width = `${percentage}%`;
    nodePercent!.textContent = `${percentage}%`;

    if (percentage === 100) {
      nodeSuccess!.classList.remove('hidden');
    } else {
      nodeSuccess!.classList.add('hidden');
    }
  }

  nodeCheckboxes.forEach(cb => {
    cb.addEventListener('change', updateNodeProgress);
  });

</script>
