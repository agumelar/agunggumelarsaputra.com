---
import type { UserSessionPayload } from '../../utils/auth';

interface Props {
  user: UserSessionPayload | null;
}

const { user } = Astro.props;
---

<div class="interactive-material-p8 space-y-8" id="interactive-p8-root">

  <!-- SCENE 1: Audit Evidence Checklist -->
  <section class="p-5 rounded-2xl bg-gradient-to-br from-emerald-500/10 via-green-500/5 to-transparent border border-emerald-500/20 shadow-sm relative overflow-hidden">
    <div class="absolute top-0 right-0 w-32 h-32 bg-emerald-500/10 blur-3xl rounded-full"></div>
    
    <div class="relative z-10 space-y-4">
      <div class="flex items-center gap-2">
        <span class="p-2 rounded-lg bg-emerald-500/20 text-emerald-600 dark:text-emerald-400 font-bold text-xs font-mono">SCENE 01</span>
        <h3 class="text-lg font-bold text-foreground">Audit Evidence OR-01</h3>
      </div>
      
      <p class="text-xs text-muted-foreground leading-relaxed">
        Periksa dua karya sebelum diajukan untuk validasi. Evidence kuat memadukan isi yang lengkap dan presentasi yang rapi.
      </p>

      <div class="space-y-3" id="audit-checklist">
        <!-- Checklist items -->
        <label class="flex items-start gap-3 p-4 rounded-xl border border-border bg-card hover:bg-emerald-500/5 cursor-pointer transition-all group">
          <input type="checkbox" class="audit-cb mt-1 h-5 w-5 rounded border-border text-emerald-600 focus:ring-emerald-500" data-index="0" />
          <div>
            <div class="font-bold text-sm group-hover:text-emerald-600 dark:group-hover:text-emerald-400 transition-colors">Mind Map Profesi</div>
            <p class="text-xs text-muted-foreground mt-1">Siapkan PDF mind map profesi PPLG. Ini evidence pertama OR-01.</p>
          </div>
        </label>
        
        <label class="flex items-start gap-3 p-4 rounded-xl border border-border bg-card hover:bg-emerald-500/5 cursor-pointer transition-all group">
          <input type="checkbox" class="audit-cb mt-1 h-5 w-5 rounded border-border text-emerald-600 focus:ring-emerald-500" data-index="1" />
          <div>
            <div class="font-bold text-sm group-hover:text-emerald-600 dark:group-hover:text-emerald-400 transition-colors">Rencana Minat</div>
            <p class="text-xs text-muted-foreground mt-1">Siapkan PDF rencana minat belajar tiga tahun. Roadmap perlu berisi alasan dan target.</p>
          </div>
        </label>

        <label class="flex items-start gap-3 p-4 rounded-xl border border-border bg-card hover:bg-emerald-500/5 cursor-pointer transition-all group">
          <input type="checkbox" class="audit-cb mt-1 h-5 w-5 rounded border-border text-emerald-600 focus:ring-emerald-500" data-index="2" />
          <div>
            <div class="font-bold text-sm group-hover:text-emerald-600 dark:group-hover:text-emerald-400 transition-colors">Kelengkapan Profesi</div>
            <p class="text-xs text-muted-foreground mt-1">Pastikan profesi, tugas, dan tools terbaca. Rubrik menghargai isi yang lengkap.</p>
          </div>
        </label>

        <label class="flex items-start gap-3 p-4 rounded-xl border border-border bg-card hover:bg-emerald-500/5 cursor-pointer transition-all group">
          <input type="checkbox" class="audit-cb mt-1 h-5 w-5 rounded border-border text-emerald-600 focus:ring-emerald-500" data-index="3" />
          <div>
            <div class="font-bold text-sm group-hover:text-emerald-600 dark:group-hover:text-emerald-400 transition-colors">Keterbacaan Visual</div>
            <p class="text-xs text-muted-foreground mt-1">Periksa tata letak, warna, dan teks. Evidence harus nyaman dibaca.</p>
          </div>
        </label>

        <label class="flex items-start gap-3 p-4 rounded-xl border border-border bg-card hover:bg-emerald-500/5 cursor-pointer transition-all group">
          <input type="checkbox" class="audit-cb mt-1 h-5 w-5 rounded border-border text-emerald-600 focus:ring-emerald-500" data-index="4" />
          <div>
            <div class="font-bold text-sm group-hover:text-emerald-600 dark:group-hover:text-emerald-400 transition-colors">Folder Rapi</div>
            <p class="text-xs text-muted-foreground mt-1">Satukan karya dalam folder OR-01. Struktur rapi memudahkan guru memvalidasi.</p>
          </div>
        </label>
      </div>

      <!-- Feedback Area -->
      <div id="audit-progress-container" class="mt-4">
        <div class="flex justify-between text-xs font-medium text-muted-foreground mb-2">
          <span>Kesiapan Audit</span>
          <span id="audit-percent">0%</span>
        </div>
        <div class="w-full bg-secondary h-2.5 rounded-full overflow-hidden">
          <div id="audit-bar" class="bg-emerald-500 h-2.5 rounded-full transition-all duration-500" style="width: 0%"></div>
        </div>
      </div>

      <div id="audit-success" class="hidden mt-4 p-4 rounded-xl bg-emerald-500/10 border border-emerald-500/30 text-center animate-in fade-in" role="status" aria-live="polite">
        <h4 class="font-bold text-emerald-600 dark:text-emerald-400">Audit Selesai! ✅</h4>
        <p class="text-xs text-muted-foreground mt-1">Karya yang baik belum siap dinilai bila penilai tidak dapat membukanya.</p>
      </div>
    </div>
  </section>

  <!-- SCENE 2: Urutkan Validasi File dan Akses -->
  <section class="p-5 rounded-2xl bg-gradient-to-br from-amber-500/10 via-orange-500/5 to-transparent border border-amber-500/20 shadow-sm relative overflow-hidden">
    <div class="absolute bottom-0 right-0 w-40 h-40 bg-amber-500/10 blur-3xl rounded-full"></div>
    
    <div class="relative z-10 space-y-4">
      <div class="flex items-center gap-2">
        <span class="p-2 rounded-lg bg-amber-500/20 text-amber-500 font-bold text-xs font-mono">SCENE 02</span>
        <h3 class="text-lg font-bold text-foreground">Urutkan Validasi Akses</h3>
      </div>
      
      <p class="text-xs text-muted-foreground leading-relaxed">
        Susun langkah agar guru dapat membuka evidence tanpa hambatan. Klik urutan yang benar dari ekspor hingga pengujian akhir.
      </p>

      <div class="space-y-2" id="val-container">
        <!-- Buttons will be generated by script -->
      </div>
      
      <div id="val-success" class="hidden p-4 rounded-xl bg-green-500/10 border border-green-500/30 text-center animate-in zoom-in" role="status" aria-live="polite">
        <div class="text-3xl mb-2">📁</div>
        <h4 class="font-bold text-green-500">Urutan Validasi Tepat!</h4>
        <p class="text-xs text-muted-foreground mt-1">File yang bagus tetap harus dapat diakses oleh penilai tanpa Access Denied.</p>
      </div>
    </div>
  </section>

</div>

<script>
  // SCENE 1 Logic (Checklist)
  const auditCheckboxes = document.querySelectorAll<HTMLInputElement>('.audit-cb');
  const auditBar = document.getElementById('audit-bar');
  const auditPercent = document.getElementById('audit-percent');
  const auditSuccess = document.getElementById('audit-success');

  function updateAuditProgress() {
    const total = auditCheckboxes.length;
    const checked = Array.from(auditCheckboxes).filter(cb => cb.checked).length;
    const percentage = Math.round((checked / total) * 100);

    auditBar!.style.width = `${percentage}%`;
    auditPercent!.textContent = `${percentage}%`;

    if (percentage === 100) {
      auditSuccess!.classList.remove('hidden');
    } else {
      auditSuccess!.classList.add('hidden');
    }
  }

  auditCheckboxes.forEach(cb => {
    cb.addEventListener('change', updateAuditProgress);
  });

  // SCENE 2 Logic (Sequence)
  const valSteps = [
    { id: 'step-1', label: '1. Ekspor PDF', detail: 'Simpan karya dalam format PDF. Menjaga tampilan dokumen.' },
    { id: 'step-2', label: '2. Terapkan nama file', detail: 'Gunakan konvensi nama evidence yang disepakati.' },
    { id: 'step-3', label: '3. Unggah ke folder', detail: 'Masukkan kedua PDF ke folder OR-01.' },
    { id: 'step-4', label: '4. Atur Public Viewer', detail: 'Pastikan link dapat dibuka tanpa Access Denied.' },
    { id: 'step-5', label: '5. Uji link', detail: 'Buka link dari mode lain sebelum mengirim.' }
  ];

  let currentValIndex = 0;
  const valContainer = document.getElementById('val-container');
  const valSuccess = document.getElementById('val-success');

  function renderValSteps() {
    valContainer!.innerHTML = '';
    valSteps.forEach((step, index) => {
      const isLocked = index > currentValIndex;
      const isCompleted = index < currentValIndex;
      const isCurrent = index === currentValIndex;
      
      const btn = document.createElement('button');
      btn.type = 'button';
      btn.className = `w-full text-left p-3 sm:p-4 rounded-xl border flex items-center justify-between transition-all focus-visible:outline-amber-400 ${
        isCompleted ? 'bg-amber-500/20 border-amber-500/50 text-foreground opacity-70' :
        isCurrent ? 'bg-amber-500 text-amber-950 font-bold border-amber-600 shadow-md transform hover:-translate-y-1' :
        'bg-secondary/30 border-border text-muted-foreground opacity-50 cursor-not-allowed'
      }`;
      
      btn.innerHTML = `
        <div class="flex items-center gap-3">
          <div class="${isCompleted ? 'text-amber-500' : isCurrent ? 'text-amber-950' : 'text-muted-foreground'} font-bold">
            ${isCompleted ? '✓' : isCurrent ? '▶' : '🔒'}
          </div>
          <div>
            <div class="text-sm ${isCurrent ? 'font-bold' : 'font-semibold'}">${step.label}</div>
            ${isCompleted || isCurrent ? `<div class="text-xs ${isCurrent ? 'text-amber-900/80' : 'text-muted-foreground'} mt-1">${step.detail}</div>` : ''}
          </div>
        </div>
      `;

      if (isCurrent) {
        btn.addEventListener('click', () => {
          currentValIndex++;
          renderValSteps();
          if (currentValIndex >= valSteps.length) {
            setTimeout(() => {
              valSuccess!.classList.remove('hidden');
            }, 300);
          }
        });
      }

      valContainer!.appendChild(btn);
    });
  }

  // Initialize
  renderValSteps();

</script>
