# Task 7 Report

**Status:** DONE

**Changes Made:**
- Updated `finishQuiz()` in `src/components/TkaQuiz.astro` to add a `fetch` POST request to `/api/tka/submit` with the user's score, total questions, and correct answers.
- Committed the changes with the message: `feat: save tka exam attempts to database`.
